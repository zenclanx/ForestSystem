from flask import Flask, request, jsonify
from ultralytics import YOLO
import os

class_map = {
    'Centipedes': '蜈蚣',
    'Cockroaches': '蟑螂',
    'House-centipedes': '家蜈蚣',
    'Larvas': '幼虫',
    'Moth-flies': '蛾蝇',
    'Stink-Bugs': '臭虫',
    'House-moths': '家蛀蛾',
    'spiders': '蜘蛛'
}
# 1️⃣ 加载训练好的模型
model = YOLO("D:/Graduation Project/VueProject/znx_Graduation_Project/znx_project/backend/runs/detect/train4/weights/best.pt")

# 2️⃣ 选择要测试的图片
image_path = "D:/Graduation Project/VueProject/znx_Graduation_Project/znx_project/backend/picture/spider.jpeg"

results = model(image_path)  # 返回列表

# results[0] 才有属性 .boxes 和 .names
# 将结果对象的 names 替换为中文
results[0].names = {i: class_map.get(name, name) for i, name in results[0].names.items()}

# 显示图片
results[0].show()



# # 图片文件夹
# image_folder = r"D:/Graduation Project/VueProject/znx_Graduation_Project/znx_project/backend/pictures"

# # 保存结果文件夹
# save_folder = r"D:/Graduation Project/VueProject/znx_Graduation_Project/znx_project/backend/results"
# os.makedirs(save_folder, exist_ok=True)

# # 遍历文件夹
# for img_name in os.listdir(image_folder):
#     if img_name.lower().endswith(('.png', '.jpg', '.jpeg')):
#         img_path = os.path.join(image_folder, img_name)
#         results = model(img_path)   # 推理
#         results.save(save_dir=save_folder)  # 保存带标注的图片
#         print(f"预测完成: {img_name}")


# import torch

# # 1. 检查版本
# print(f"PyTorch 版本: {torch.__version__}")
# print(f"CUDA 版本: {torch.version.cuda}")

# # 2. 检查支持的架构 (关键!)
# print(f"支持的架构: {torch.cuda.get_arch_list()}")

# # 3. 检查 GPU 是否可用
# print(f"GPU 可用: {torch.cuda.is_available()}")
# print(f"GPU 名称: {torch.cuda.get_device_name(0)}")

# # 4. 简单计算测试
# x = torch.randn(1000, 1000).cuda()
# y = x @ x
# print(f"计算测试通过! 结果形状: {y.shape}")
